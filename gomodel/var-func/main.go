package main

import _case1 "gomodel/var-func/case1"

func main() {

	//_case1.ArithmeticCase()
	//_case1.LogicalCase()
	_case1.BitwiseCase()

	/*fmt.Println(_case.SumCase(2, 5))

	user := _case.NewUser("jack", 19)
	fmt.Println(user.GetAge(), user.GetName())
	fmt.Println(user.Age, user.Name)
	//调用方法获取属性
	//优点：
	//封装性：通过方法访问属性，可以隐藏属性的具体实现细节，只暴露出方法接口。这样可以在不改变外部代码的情况下，修改内部实现，提高了代码的可维护性和可扩展性。
	//可扩展性：可以通过方法添加额外的逻辑，例如验证、转换等。这样可以在获取属性的同时，执行一些额外的操作，提高了代码的灵活性和可读性。
	//多态性：可以通过方法实现多态性，即不同的对象可以调用相同的方法，但执行不同的逻辑。
	//缺点：
	//性能开销：调用方法通常会有一定的性能开销，因为需要执行额外的函数调用。如果性能是关键因素，可能需要避免使用方法。
	//复杂性：使用方法可能会使代码变得更加复杂，特别是当方法中包含复杂的逻辑时。

	*/

}
